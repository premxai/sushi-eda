# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.23
asyncpg==0.29.0
aiosqlite==0.19.0
psycopg2-binary==2.9.9
alembic==1.13.0

# ── Data Processing ───────────────────────────────────────────────────────────
polars>=0.20.0
pandas==2.1.3
numpy==1.26.2
scipy==1.11.4
scikit-learn==1.3.2
statsmodels==0.14.1
openpyxl==3.1.2
pyarrow==14.0.1
duckdb==0.10.0

# ── Visualization ─────────────────────────────────────────────────────────────
plotly==5.18.0

# ── Cache (optional — in-process fallback when REDIS_URL unset) ──────────────
redis==5.0.1

# ── Auth (Clerk JWT verification — auth.py uses jose + httpx directly) ────────
python-jose==3.3.0
cryptography>=41.0.7

# ── File Storage (Cloudflare R2 / S3-compatible) ──────────────────────────────
boto3==1.34.0

# ── AI ────────────────────────────────────────────────────────────────────────
anthropic==0.18.1

# ── Observability ─────────────────────────────────────────────────────────────
loguru==0.7.2
slowapi==0.1.9
sentry-sdk[fastapi]>=1.40.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.0
# <0.28: starlette's TestClient (via fastapi 0.104) still passes the removed `app=` kwarg
httpx>=0.26.0,<0.28
